import createElement from '../../utils/creators';

const header = createElement({
  tag: 'header',
  classes: ['header'],
  attributes: {
    id: 'header',
  },
  innerText: '',
});

export default header;
